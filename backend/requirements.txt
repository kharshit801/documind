# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
mangum==0.17.0

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- LangChain + integrations ---
# We drive Pinecone via its SDK directly (see services/retrieval.py) rather
# than the langchain-pinecone wrapper, which keeps the dependency tree slimmer
# and gives us full control over scores + metadata.
langchain==0.3.3
langchain-core==0.3.10
langchain-openai==0.2.2
langchain-text-splitters==0.3.0

# --- Vector DB / LLM clients ---
pinecone==5.3.1
openai==1.51.0

# --- File parsers ---
PyMuPDF==1.25.1
python-docx==1.1.2

# --- Test deps (kept here so Lambda zip can run smoke tests if desired;
# split into requirements-dev.txt if you want a slimmer prod package) ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
flake8==7.1.1
